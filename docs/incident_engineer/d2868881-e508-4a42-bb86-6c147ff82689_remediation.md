# Incident remediation: `d2868881-e508-4a42-bb86-6c147ff82689`

## RCA summary
There is a syntax error in the SQL code of the model fct_orders, specifically related to the use of the word 'ORDER'.

## Confidence score
0.9

## Validation
- Patch scope validated: **True**

### Recommended steps
- Open the SQL file located at models/marts/orders/fct_orders.sql.
- Locate the table reference 'ORDER' in the SQL code.
- Change 'ORDER' to 'ORDERS'.
- Save the changes to the file.
- Create a pull request to merge the changes into the main branch.

## Proposed patches

### Patch 1: `models/marts/orders/fct_orders.sql`
_Rename table reference from 'ORDER' to 'ORDERS'._

```
with source_data as (
    select *
    from ECOMMERCE_DB.RAW.ORDERS
)

select
    order_id,
    customerid,
    order_date,
    status
from source_data
```
