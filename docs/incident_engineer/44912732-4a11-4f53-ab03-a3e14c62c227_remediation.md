# Incident remediation: `44912732-4a11-4f53-ab03-a3e14c62c227`

## RCA summary
Duplicate row detected during DML action in the products model.

## Confidence score
0.9

## Validation
- Patch scope validated: **True**

### Recommended steps
- manual_review_required
- Review patches on draft PR branch
- Run dbt build --select affected_model
- Do not auto-merge

## Proposed patches

### Patch 1: `models/products/products.sql`
_Remove duplicate SELECT statement to prevent duplicate rows._

```
{{ config(
    materialized='incremental',
    unique_key='product_id'
) }}

WITH duplicated_data AS (

    SELECT
        product_id,
        product_name
    FROM RAW_DB.RAW_SCHEMA.PRODUCTS
)

SELECT *
FROM duplicated_data
```
