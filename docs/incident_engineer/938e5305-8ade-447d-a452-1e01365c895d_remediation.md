# Incident remediation: `938e5305-8ade-447d-a452-1e01365c895d`

## RCA summary
The SQL compilation error occurred due to the non-existence of the specified warehouse 'NON_EXISTENT_WH' or the suspended state of the warehouse 'COMPUTE_WH'.

## Confidence score
0.9

## Validation
- Patch scope validated: **True**

### Recommended steps
- manual_review_required
- Review patches on draft PR branch
- Run dbt build --select affected_model
- Do not auto-merge

## Proposed patches

### Patch 1: `models/staging/stg_customers.sql`
_The warehouse 'NON_EXISTENT_WH' is not valid. Change to 'COMPUTE_WH' and ensure the warehouse is resumed before running the model._

```
{{
  config(
    warehouse='COMPUTE_WH',
    materialized='table'
  )
}}

-- Force the warehouse switch
{% do run_query("USE WAREHOUSE COMPUTE_WH") %}

with source_data as (
    select *
    from ECOMMERCE_DB.RAW.CUSTOMERS
)
select *
from source_data
```
