# Incident remediation: `7333ff7d-4b3e-4277-a24a-249715ef1d25`

## RCA summary
The identifier 'CUSTOMERID' is not found in the source table 'CUSTOMERS_MISSING'.

## Confidence score
0.75

## Validation
- Patch scope validated: **True**

### Recommended steps
- manual_review_required
- Review patches (max small diff policy)
- Apply locally or via draft PR only
- Run dbt build --select affected_model
- Do not auto-merge

## Proposed patches

### Patch 1: `models/marts/orders/schema.yml`
_Document and declare column 'CUSTOMERID' for model 'fct_orders' (warehouse may still need ALTER)._

```diff
--- a/models/marts/orders/schema.yml
+++ b/models/marts/orders/schema.yml
@@ -1,6 +1,11 @@
 version: 2
 models:
   - name: fct_orders
     columns:
       - name: CUSTOMERID
         description: "Added to resolve missing identifier / schema mismatch"
```
